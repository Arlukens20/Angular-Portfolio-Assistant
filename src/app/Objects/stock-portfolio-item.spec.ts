import { StockPortfolioItem } from './stock-portfolio-item';

describe('StockPortfolioItem', () => {
  it('should create an instance', () => {
    expect(new StockPortfolioItem()).toBeTruthy();
  });
});
